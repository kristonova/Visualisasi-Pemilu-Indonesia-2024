# Legacy utilities

`build_kec_index.py` belongs to the retired name/code-based GIS loader. The
current dashboard resolves every feature through the exact KPU hierarchy key
and does not consume `data/gis/kec_index.json`.

The file remains here only to make older repository history reproducible. Do
not include it in the current build or GitHub Pages deployment.
